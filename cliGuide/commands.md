# Essential Git CLI Commands

## Git Commands

- `git status`: Check the status of what branch you are currently on, what files are tracked/untracked, history of commits
- `git clone <url>`: Clone repository to the current directory location
- `git checkout -b <branchname>`: Create new branch and switch to the branch
